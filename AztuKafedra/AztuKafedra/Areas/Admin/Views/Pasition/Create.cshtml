@model Pasition
<div class="container">
    <div class="card p-4">
        <form method="post" enctype="multipart/form-data">
            <div class="form-group">
                <label asp-for="Name"></label>
                <input asp-for="Name" class="form-control">
                <span asp-validation-for="Name" class="text-danger"></span>

                <div class="form-group">
                    <label asp-for="ChildCategoryId"></label>
                    <select asp-for="ChildCategoryId" class="form-control">
                        <option value="">Seçiniz</option>
                        @if (ViewBag.Pasitions != null)
                        {
                            foreach (SelectListItem product in ViewBag.Pasitions)
                            {
                                <option value="@product.Value">@product.Text</option>
                            }
                        }
                    </select>
                    <span asp-validation-for="ChildCategoryId"></span>
                </div>
            </div>
            <button class="btn btn-success" type="submit">Submit</button>
        </form>
    </div>
</div>
